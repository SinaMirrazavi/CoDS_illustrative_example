%% Setup paths
setup_cods;



%% Constructing the conatct surface
limits = [-5 5 -5 5];
disp('Draw the contact surface')
[N_x,Poly,check]=Construct_the_surface(limits);
if check==0
      error('Program exit')
end

% %% Constructing the dynamcail system
% disp('Draw some motions, make sure that it ends up at the target point and it goes through the contact surface !')
% [A,Dem_i,check]=Construct_the_dynamcial_system(N_x,Poly,limits);
% if check==0
%       error('Program exit')
% end
% 
% %% Simulate the unmodulated system
% simulate_modulated_system(A,N_x,Dem_i);
% 
% simulate_unmodulated_system(A,N_x,Dem_i);